<%= form_for(@navbar) do |f| %>
  <% if @navbar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@navbar.errors.count, "error") %> prohibited this navbar from being saved:</h2>

      <ul>
      <% @navbar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
